extends base
include mixins/imgboard

block content
    div
        h2 이미지 모음
        each imgboard in imgBoardsSp
            +imgboard(imgboard)
        nav 
            -for(var i = 0; i< div5; i++)
                li  
                    a(href=`/page/${i}`) #{i+1}
        